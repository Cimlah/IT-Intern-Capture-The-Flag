# Host port mapped to the hub's SSH server (change if 2222 is taken)
CTF_SSH_PORT=2222

# Optional: override the mentor password (otherwise one is generated at spin-up)
# CTF_MENTOR_PASSWORD=

# Optional: allow the "reset progress" action from inside the Ink app
# CTF_ALLOW_RESET=1
